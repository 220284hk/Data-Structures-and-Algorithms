package myImplementationsW4;

public class main {


//    fun solution(n: Int, delivery: Array<IntArray>): String {
//        var answer = ""
//        return answer
//    }
}

//    SELECT A.ID, A.CART_ID, B.NAME, B.PRICE FROM (SELECT * FROM CART_PRODUCTS LIMIT 10) AS A, CART_PRODUCTS AS B
//        LIMIT 10;

//    SELECT A.CART_ID, A.NAME, B.CART_ID, B.NAME
//        FROM (SELECT CART_ID, NAME FROM CART_PRODUCTS
//        WHERE CART_ID = 83 GROUP BY NAME) AS A,
//        (SELECT CART_ID, NAME FROM CART_PRODUCTS
//        WHERE CART_ID = 83 GROUP BY NAME) AS B;
